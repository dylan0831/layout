<%= form_with(model: parkson, local: true) do |form| %>
  <% if parkson.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(parkson.errors.count, "error") %> prohibited this parkson from being saved:</h2>

      <ul>
      <% parkson.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
